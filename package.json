{
  "name": "lfw-token",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ duyhotan2000/lfw-token",
    "create-local": "graph create --node http://localhost:8020/ duyhotan2000/lfw-token",
    "remove-local": "graph remove --node http://localhost:8020/ duyhotan2000/lfw-token",
    "deploy-local": "graph deploy --node http://localhost:8020/ duyhotan2000/lfw-token"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
